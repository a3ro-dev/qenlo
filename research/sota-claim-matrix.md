# SOTA claim matrix

Scope date: 2026-09-17. Verdicts use only retained evidence, the audit ledgers,
and cited primary sources. A claim may be narrow and true; broad claims fail
here. `Promising` means evidence exists but a defined gate has not passed.
`Falsified` means retained cells directly contradict the claim. All cohort
labels match `paper/paper.tex` §5.

| # | Candidate claim | Comparison set and why | Metric, boundary, gate | Workloads | Evidence present | Strongest counterevidence | Missing falsifying experiment | Verdict |
|---|---|---|---|---|---|---|---|---|
| C1 | Qenlo is durable: canonical records survive reopen, and atomic mixed commits validate before publication | Qenlo's own store versus its recovery tests, not external systems | Automated recovery/corruption test suites; exit code | Small/medium local collections | `crates/qenlo` recovery and process-exit tests; `docs/verification.md` records; `docs/recovery-policy.md` | No power-loss guarantee on Windows publication; no arbitrary crash schedules, network filesystems, or concurrency | Hardware power-cut matrix on named filesystems; preregistered in `gap-ranked-plan.md` G6 | `supported` for tested schedules and reopen paths; `not comparable` as a general durability ranking |
| C2 | Qenlo gives exact filtered cosine search with deterministic ordering in-process | Qenlo CPU path versus its FP64 oracle and recall/eligibility gates | Oracle recall, ID/eligibility validation; per-run P95 host-call timing | 100K×384 native cohorts; small campaign cells | H1 native crossover, S1/S2 recall-1.0 rows, CPU-quality tests | FP32 storage; dense historical rows recall 0.99998; not every arithmetic step is FP64 | Exhaustive-scale FP64 agreement study across dims | `supported` as algorithmic exhaustiveness, `unsupported` as FP64 numerical identity |
| C3 | One-pass/cached eligibility lowers completed-call P95 | H2 RTX 4090 five-run sequential modes | Median run P95, native completed-call | 100K×384, E∈{1K,4K,6K,100K} | `research/artifacts/qenlo-phase1-eligibility-2026-09-04.tar.gz` | No CPU cells; cannot recalibrate H1 crossover | Same-host CPU+GPU factorial with mutation | `supported` within H2; `not comparable` across cohorts |
| C4 | Neither E nor EDB is a universal routing scalar | H1 RTX 4050 vs P0 RTX 4090 (E-only); R1 16 preregistered workloads (EDB) | Median forced-route P95 regret vs 25% limit | Named cells only | `research/data/processed/archive-reanalysis/`; router archive `8e3b5fe6…` | Post-hoc EDB threshold ≈1.5M: 1/16 errors, 3.16% max regret — so failure is threshold-specific, not factorization-impossibility | Fresh suite with separate dev/held-out split; per-query pairing | `falsified` for the frozen 1e6 EDB rule and any single monotone E threshold on these cells; `promising` that no monotone EDB rule survives: a same-host post-hoc witness (E0/E2, `edb_ordering_witness.csv`: GPU wins at 614,400, CPU at 1,152,000 in 9/10 blocks) replicates the ordering contradiction but was not preregistered as an EDB test |
| C5 | Automatic path beats forced best route | R1 automatic vs faster forced P95 | Auto/best ratio 0.412–5.393 | R1 16 workloads | Router archive | Both directions occur; no causal attribution | Interleaved same-session forced+automatic runs | `falsified` as a claim; they are not interchangeable |
| C6 | WGPU beats CPU dense/batched | H1, S2, P0, H6/H7 | Completed-call P95 | Named cells | See cohorts | Reverses with shape and environment; S2 used rejected candidate | Controlled factorial | `falsified` as universal; `supported` only per named cell |
| C7 | Qenlo CPU competitive with optimized CPU libraries | P2 A40 FAISS Flat single-thread: FAISS 11.154 vs Qenlo 15.250 ms, recall 0.99984 vs 0.99998 | Same corpus/batch, separate runs | 100K×384 dense B=1 | `archive-reanalysis/phase2_cpu_optimization.csv` | One cell, one library configuration; no sqlite-vec, no Qenlo-side optimization rerun | Time-matched same-host library matrix incl. sqlite-vec | `falsified` for dense B=1 vs FAISS Flat; `not comparable` elsewhere |
| C8 | WGPU 0.897/0.896 ms P95 (768D) | S2 archive, candidate selector | Completed-call P95, FP64 recall 1.0 | 100K×768 B=1/8 | Campaign matrix + raw archive | Candidate was rejected; local source not rerun | Rerun with final selector | `unsupported` for the current source; historical observation only |
| C9 | Chroma/USearch/FAISS comparisons | H0/H3/S1 rows | P95 at stated recall | Named cells | Windows/Linux/campaign artifacts | Different APIs, revisions, storage; ANN recall <1.0 | Matched-API frontier | `not comparable` for ranking; valid as scoped operating points |
| C10 | Android/iOS physical performance current | H6 aggregates (app 0.1.0, commit unrecorded) | Aggregate P95, no raw calls | 10K/100K×384 | `research/data/raw/2026-09-02-android/` | No current-revision packaging or thermal/power control | Fresh device-lab run on current source | `unsupported` for current source; historical context only |
| C11 | Product-ready durability: WAL + snapshot + generation watermark, checksummed reopen | Qenlo store vs own tests, not competitors | Test pass + format version checks | Local collections | `docs/verification.md`; qn-format docs | Not a benchmarked comparison | Peer comparison of recovery semantics | `supported` as design fact, `not comparable` as SOTA |
| C12 | No external engine tested yet is a strict superset of Qenlo's combined durable+exact+filtered embedded contract | Embedding stores: sqlite-vec (no native durable transactional store documented at this scope), Chroma (client-server orientation, Ephemeral/Local modes), LanceDB (columnar, exact mode limited), FAISS/hnswlib/USearch (libraries, not stores) | Feature/contract inspection against official docs | Integration-scoped, not perf | Docs cited in `related-work-current.md` | Docs change; version drift; vendor roadmaps | Version-pinned feature audit | `promising` as a design-space claim; `unsupported` as a performance claim |
| C13 | Eligibility representation is a first-order routing variable | E2 same-host gpu-rows vs gpu-predicate vs gpu-mask (RTX 4090/Vulkan, partial archive e4ca1336) | Paired block-bootstrap P50/P95 ratio; conservative E0 A/A screen | 100K×384, B∈{1,16}, f=0.001–1 | `runpod-e0-e2-analysis-20260924/paired_comparisons.csv` | Predicate faster at B=1/f=1 (not clearing screen); B=16 f=1 missing; one host/corpus/predicate shape | Complete E2; second device; automatic arm | `supported` for sparse cells on this host (3.2–6.1×); `unsupported` as a general default |
| C14 | Same-cell measurement noise is small relative to routing gates | E0 10-block A/A; R1 automatic vs same-backend A/A | Block ratio distribution vs 25% gate | B=1/E=3,000 (E0); R1 16 cells | `e0_noise.csv`; `router-noise-audit/summary.json` | — | Continuous clock telemetry; E0 at more cells | `falsified`: A/A ratios reach ~2× (E0) and exceed 25% in 9/16 R1 cells |

Explicit non-claims: fastest vector engine, best filtered search, production
ready, universal CPU/GPU threshold, validated adaptive router, portable
performance across devices. Qenlo's most defensible identity remains an
embedded, durable, exact, inspectable store with conditional execution —
not the fastest vector engine.
